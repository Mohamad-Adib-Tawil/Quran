import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF0CAF60); // green from designs
  static const Color primaryDark = Color(0xFF0A8A4C);
  static const Color scaffoldLight = Colors.white;
  static const Color scaffoldDark = Color(0xFF121212);
  static const Color textDark = Color(0xFF0E0E0E);
  static const Color textLight = Colors.white;
  static const Color accent = Color(0xFF00A676);
}
