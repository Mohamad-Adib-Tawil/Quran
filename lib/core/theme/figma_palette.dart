import 'package:flutter/material.dart';

/// Colors extracted from Figma nodes (approximate where necessary)
class FigmaPalette {
  FigmaPalette._();

  // Primary brand color from Figma (#03AA77)
  static const Color primary = Color(0xFF03AA77);
  static const Color primaryDark = Color(0xFF048A62);

  // Neutrals
  static const Color white = Colors.white;
  static const Color textDark = Color(0xFF222222);
  static const Color divider = Color(0xFFE3E3E3);

  // Surfaces
  static const Color surface = Colors.white;
  static const Color surfaceMuted = Color(0xFFF6F6F6);
}
